# Copy to `.env` (local) or `.dev.vars` (wrangler) and fill in.
PUBLIC_SITE_URL=https://wayward.house
PUBLIC_CF_ANALYTICS_TOKEN=

# Cloudflare deploy (only needed if running `npm run deploy` outside CI)
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
